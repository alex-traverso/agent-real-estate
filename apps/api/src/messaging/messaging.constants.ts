/**
 * Constants for the Meta Cloud API (WhatsApp) outbound sender.
 * The Graph API version is pinned explicitly so upgrades are a deliberate,
 * reviewed change rather than an implicit drift.
 */

export const GRAPH_API_BASE_URL = 'https://graph.facebook.com';
export const GRAPH_API_VERSION = 'v21.0';
export const WHATSAPP_MESSAGING_PRODUCT = 'whatsapp';
